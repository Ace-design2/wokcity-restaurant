import React from 'react';

export default function Hero() {
  return (
    <section className="w-full h-96 bg-gradient-to-b from-red-950 to-transparent shrink-0">
      {/* Visual background section only, no text as per requirements */}
    </section>
  );
}
